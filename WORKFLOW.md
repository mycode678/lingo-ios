# 开发闭环：让验证变成闸门，人不必一直在场

> 目标：用户只在**方案层面**出现，不再当测试员。
> 每一步该由谁验、验什么、什么条件才算过，全部写死在这里。

---

## 四道闸门

一个任务做完，必须依次过完四道闸。**任何一道不过，回去改，重新走一遍。**

### G0 机器闸（无人参与）

Mac 上一条命令跑完：

```
拉代码 → 校验提交号 → 编译签名 → 装真机 → 12 条 UI 测试 → 布局体检 → 导出截图
```

过闸条件（全部满足）：
- 编译的**必须是我刚推的那个提交**（`~/lingo-want` 对不上直接停）
- 12 条测试全绿
- 体检报告里没有 `LAYOUT-FAIL`（重叠 / 超出屏幕）
- 截图导出成功

### G1 代码闸（`code-critic`）

只收"当 ___ 的时候，会 ___"这种能复现的缺陷，不收风格意见。
**有 P1 或 P2 一律打回。** P3 记进待办，可以不当场修。

### G2 界面闸（`ui-critic`）

拿真机截图 + 布局体检数字，按 iOS HIG 和用户定的设计原则挑。
**元素被切、说不清的空白、间距不足、对比度不够、触摸目标小于 44 点 —— 一律打回。**

G1 和 G2 **并行**跑，互不等待。

### G3 需求闸（`spec-auditor`）

不是每个任务都过，**每个阶段收尾时过一次**。
拿 `PLAN.md` 的用户原话去对代码，产出差集清单 → 差集直接变成下一批任务。

---

## 循环怎么转

```
从 TASKS.md 取一个任务
      ↓
  做（只做这一个）
      ↓
   G0 机器闸  ──不过──┐
      ↓ 过            │
 G1 ‖ G2 并行 ─不过──┤
      ↓ 全过          │
 在 TASKS.md 打勾      └─→ 改，重新走
      ↓
 还有任务？──有──→ 回到取任务
      ↓ 没有
   G3 需求闸 → 差集变成新的 TASKS.md
```

---

## 什么时候必须叫用户

只有三种，别的一概不打扰：

1. **方案有歧义** —— 两种解读会做出完全不同的东西
2. **要花钱、上架、对外发布** —— 不可逆的事
3. **同一个问题修了三轮还不过** —— 说明我理解错了，继续修是浪费

---

## 防自欺条款

以下每一条都是**今天真栽过**才写进来的，不是假想：

| 栽过的坑 | 现在靠什么挡住 |
|---|---|
| Mac 拉不到 GitHub，悄悄拿上一版编译测试截图，全绿但改的东西根本没进去 | `~/lingo-want` 写死这一轮该跑哪个提交，对不上直接停 |
| 布局体检量出了重叠，但只 `print` 不断言，测试照样绿 | `LAYOUT-FAIL` 进探针标签 → 测试断言 → 红 |
| 横屏滚动测试盯的元素改完顺序后一进来就在屏幕里，一次不用滑就通过，横屏彻底滚不动也是绿的 | 只盯**最后一个**元素；断言必须真的可能失败 |
| 体检探针只写在竖屏，横屏（最容易挤重叠）永远"没拿到"，还不算失败 | 探针提到两个方向共用；拿不到 = 测试红 |
| 改完不看截图就说"好了"，让用户去发现 | 截图自动导出，我自己先逐张看完才允许汇报 |
| 凭压缩摘要复述用户原话，复述岔了两遍 | 一律翻 `PLAN.md` 或原始会话记录，不许凭记忆 |

---

## 三个 agent

定义在 `.claude/agents/`，跨会话可用：

| agent | 管什么 | 打回标准 |
|---|---|---|
| `code-critic` | 会真出错的逻辑缺陷 | P1/P2 |
| `ui-critic` | 截图 + 体检数字，HIG 与设计原则 | 被切/空白/间距/对比度/触摸目标 |
| `spec-auditor` | 说好的做到没有 | 出现"没做"或"半做" |
