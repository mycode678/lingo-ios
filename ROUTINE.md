# 定时任务要做的事

> 这份是给**云端定时任务**（每 4 小时跑一次的那个 routine）看的。
> 它每次醒来读这一份，照着做。人不用看。

## 第一步：先判断还有没有活

读 `TASKS.md`。**如果所有任务都打了勾、也没有留下「还差」「未做」「等真机」之外的条目，
就什么都别做**，回复「已完成，无需处理」然后结束。

**不要为了有事干而制造工作。** 没活就是没活。

## 第二步：有活的话，怎么干

1. **一次只做一件**（`TASKS.md` 里最靠前的那条未完成的）。做完立刻验，不要攒着。
2. 改完 `git push` 到 `main`，然后**等 GitHub Actions 的「G0 验证（模拟器）」跑完**，
   用 GitHub API 查这一轮的 `conclusion`。红了就修到绿，别留着。
3. 做完在 `TASKS.md` 里打勾，写清楚验收结果。

## 第三步：你做不了的，别硬来

你跑在云端，**碰不到**作者的 Mac、他的 iPhone、家里的 191 服务器。所以：

- **真机那道闸你跑不了**，只能跑 GitHub Actions 的云端模拟器。
- 需要真机才能验的（录音、麦克风、锁屏控制、对齐模型、iPad 布局），
  在 `TASKS.md` 里标注「等真机」并跳过。**不要假装验过。**

## 四条硬规矩（都是栽过跟头才写下来的）

1. **不许让 `-demo` 演示数据盖住真数据。**
   判断一律写成"没装材料包时才用演示数据"（`if catalog.packs().isEmpty`），
   **绝不写 `if Demo.on`**。
   之前写反了：所有测试跑的都是假句子假波形，闸门全绿，而用户根本用不了。

2. **「测试全绿」不等于「用户能用」。**
   汇报前先自问：这条真实路径，有没有一条测试真的走过？没有就不许说全绿。

3. **Swift 字符串里的中文引号要用「」，不要用 ASCII 双引号**，
   否则字符串被截断，编译报一堆 `cannot find 'xxx' in scope`。
   改完先跑 `python3 ios/check-quotes.sh`。

4. **不许出现上传录音的代码路径**（隐私红线，`ios/guard-privacy.sh` 在 CI 里守着）。

## 写代码的风格

照抄仓库现有的：中文注释，解释**为什么这么写**而不是"这行做了什么"，
尤其写清楚踩过什么坑。提交信息用中文，说清改了什么、为什么。

## 汇报

最后用中文简短说三句：做了什么、验证结果如何、还剩什么。不要长篇大论。
