# 任务拆解

> 来源：`PLAN.md`（用户原话）→ `ARCHITECTURE.md`（怎么落）→ 拆成这里的任务。
> 走法见 `WORKFLOW.md`。**一次只做一个**，四道闸全过才打勾。
>
> 每条都写了**验收标准**。写不出机器能验的标准，就说明这个任务拆得还不够细。

---

## 阶段 A · 立骨架（只搬家，不改行为）

- [x] **A1 文件按分层归位**
  验收：`ios/Sources/` 下只剩 6 个目录；编译通过；12 条测试全绿。

- [ ] **A2 SQLite 封装 + `user.sqlite` + `AppEnv` 注入**
  做：`Stores/DB.swift`（open/migrate/exec/query，带版本号迁移）；
  建 `user.sqlite` 六张表；`App/AppEnv.swift` 从根注入，Service 层从它拿依赖。
  验收：新增一条测试 —— 写一条进度、杀掉 App 重开、读回来值一样；
  迁移跑两遍不出错（幂等）。

---

## 阶段 B · 数据掉头（服务器变成可选）

- [ ] **B1 练习进度与复习排期改成本地**
  做：`Services/PracticeService`，SM-2 排期算在本机。
  验收：断网状态下打分、算出下次复习时间、重开 App 还在。

- [ ] **B2 收藏、难点改成本地**
  验收：断网收藏一句、标一个难点，重开还在；`Api` 一次都没被调用（用打桩验）。

- [ ] **B3 录音索引改成本地**
  做：录音文件本来就只在本机，把索引也落 `user.sqlite`。
  验收：全局搜不到任何上传录音的代码路径（这是方案红线，用 grep 钉死）。

- [ ] **B4 `Api` 降级成可选备份通道**
  验收：**飞行模式下把 12 条测试跑一遍全绿** —— 这一条过了才算真脱离服务器。

---

## 阶段 C · 材料包

- [ ] **C1 打包工具（跑在 201 上）**
  做：一批音频 → 转写 → 强制对齐 → 难度分级 → 出 `pack.zip`
  （manifest.json + pack.sqlite + audio/）。
  验收：拿现有朗文例句跑一遍，出包；抽 20 句人工核对词边界误差 < 50 毫秒。

- [ ] **C2 App 端装包**
  做：下载、校验和、`ATTACH`、按包浏览、删包。
  验收：装一个包 → 断网能练 → 删包 → 用户数据（收藏进度）一条不丢。

- [ ] **C3 朗文包只有我自己能装**
  验收：普通账号装不上（Service 层入口就挡住，不是界面藏）；自己的账号能装。

---

## 阶段 D · 功能（这时每个都只是加一个 Service 加一屏）

- [ ] **D1 AI 拆解 + 8445 上的 API key 页面**
  做：DeepSeek / OpenRouter；key 在网页上填、能测可用性；额度走 `EntitlementService`。
  验收：填错 key 有明确报错；免费用户超额有拦截；断网时降级成只给 GOP 结果不崩。

- [ ] **D2 分级材料列表 + 按身份推荐**
  做：小学课本 / 雅思 / TED / 日常口语 / VOA 慢速 / VOA 标准；可预览再决定下不下。
  验收：选身份 → 列表跟着变；每个包都有难度标；预览不用先下整包。

- [ ] **D3 分级听力训练（七个练法）**
  验收：练法逻辑能单元测试（不用开模拟器）；出题句子难度符合"朗文例句那个水平"的约束。

- [ ] **D4 会员 / 额度 / 奖励 / 分享海报**
  做：连续包月 / 连续包年 / 买断三种；额度按 PLAN.md 的数（每周 50 句、每日 10 句）；
  达标出一张带 app 信息的海报，能分享到微信/朋友圈/QQ/小红书/微博。
  验收：StoreKit 沙盒里三种都能买通；额度跨天正确重置；海报在最小和最大字号下都不糊。

- [ ] **D5 教程 + 配套练习**
  做：先放 201；理论结合实际、拿真音频举例、可点击播放。
  验收：每一节都有可播的真音频；每一节都有配套练习。

- [ ] **D6 YouTube 词级跟随**
  做：只做词级跟随界面，音视频在用户自己手机和网络上。
  先研究「每日英语听力」的做法（截图在 `ting1`）。
  验收：不下载视频也能词级跟随；跟随误差肉眼看不出。

- [ ] **D7 隐私政策 + 用户协议**
  验收：写明录音只在本机、决不训练模型。

---

## 需要用户拍板的（我不自己决定）

- [ ] Google 广告在国内 iOS 上到底能不能正常展示 —— 他说"先记下研究清楚之后再说"
- [ ] 各档会员具体给多少额度 —— 他说"具体的以后再说订多少"
- [ ] 上架、定价、软著、ICP 备案的时间点
