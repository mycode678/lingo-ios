# 任务拆解

> 来源：`PLAN.md`（用户原话）→ `ARCHITECTURE.md`（怎么落）→ 拆成这里的任务。
> 走法见 `WORKFLOW.md`。**一次只做一个**，四道闸全过才打勾。
>
> 每条都写了**验收标准**。写不出机器能验的标准，就说明这个任务拆得还不够细。

---

## 阶段 A · 立骨架（只搬家，不改行为）

- [x] **A1 文件按分层归位**
  验收：`ios/Sources/` 下只剩 6 个目录（达标）；编译通过；全部 UI 测试绿。
  闸门证据：提交 `b4c230c`（26 个文件全是纯 rename，`git show --stat` 里全部 `| 0`）；
  真机跑 `00:48:07` 那一轮，12 条全绿、体检无 LAYOUT-FAIL。
  注：验收标准里别再写死"12 条"——测试条数会变，写"全绿"。

- [x] **A2 SQLite 封装 + `user.sqlite` + `AppEnv` 注入**
  已做：`Stores/DB.swift`、`App/AppEnv.swift`、`App/DBSelfTest.swift`；
  `DBUITests.testLocalDBSurvivesRelaunch` 钉住（写一条 → terminate → 重开读回来值一样；
  迁移跑两遍幂等；中文原样存取）。提交 `0a52d94`，真机 `00:59:20` 那一轮通过。
  遗留：目前除自检外还没有任何业务代码读写它 —— 那是 B1/B2/B3 的事。
  做：`Stores/DB.swift`（open/migrate/exec/query，带版本号迁移）；
  建 `user.sqlite` 六张表；`App/AppEnv.swift` 从根注入，Service 层从它拿依赖。
  验收：新增一条测试 —— 写一条进度、杀掉 App 重开、读回来值一样；
  迁移跑两遍不出错（幂等）。

---

## 阶段 B · 数据掉头（服务器变成可选）

- [x] **B1 练习进度与复习排期改成本地**
  做完：`Services/PracticeService`，SM-2 排期算在本机（提交 `15ae57d`）。
  另补：每天练了多少也落本机（`day` 表，库版本 v5）——
  「今天」那一屏的连续天数以前找服务器要，断网就把用户攒的连打卡显示成 0。

- [x] **B2 收藏、难点改成本地**（提交 `2d8ac13`）
  验收：断网收藏一句、标一个难点，重开还在；`Api` 一次都没被调用（用打桩验）。

- [x] **B3 录音索引改成本地**（提交 `9e57397`；`guard-privacy.sh` 在 CI 里守着）
  做：录音文件本来就只在本机，把索引也落 `user.sqlite`。
  验收：全局搜不到任何上传录音的代码路径（这是方案红线，用 grep 钉死）。

- [x] **B4 `Api` 降级成可选备份通道**
  做完（上）：`-nonet` 断网演练，服务器全挂时核心流程照常（提交 `f3351c0`）。
  做完（下）：「今天」那一屏最后两处服务器调用（`Api.counts` / `Api.heat`）
  改成本机算 —— 这是界面层里最后一处"没网就显示错数字"的地方。
  验收：`OfflineUITests` 在 `-nonet` 下全绿。

---

## 阶段 C · 材料包

- [x] **C1 打包工具（跑在 191 上，不是 201 —— 音频和对齐引擎都在那台）**
  做完：`/opt/dict/tools/mkpack.py` 出包、`checkpack.py` 验包。
  验收结果：朗文入门包 200 句 1.7MB 出包成功；**逐句**验过校验和、音频、
  词序跟原文一致、时间戳单调不越界（比原定的"抽 20 句人工核对"更严）。
  记一笔：朗文例句的难度分布偏中间（抽 3000 句：L1 152 / L2 1408 / L3 1226
  / L4 213 / L5 1）。这不是算法有问题 —— 词典例句本来就都不难，
  真正要分级的是以后的 VOA、TED 那类材料。
  还差：中文译文（cn 字段现在是空的，要从词典 html 里抽，单列一条）。

- [x] **C1b 材料包补中文译文**
  做完：`tools/buildcn.py` 建 `cnmap.sqlite`（86038 句），`mkpack.py` 出包时填 cn。
  验收结果：朗文入门包 200/200 句都有译文；`checkpack.py` 现在会在译文覆盖率
  低于 90% 时判失败。全库覆盖率 99.4%（86038/86577）。

- [x] **C2 App 端装包**
  做完：`Stores/PackZip.swift`（自写的最小 zip 读取器，只认不压缩条目，防 zip slip）、
  `Services/CatalogService.swift`（装/列/查/删）。
  验收结果：UI 测试 `packInstall` / `packRemove` 钉住 ——
  装包后句数、词边界、音频文件都对；删包后材料删干净、用户数据一条不少。

- [x] **C3 朗文包只有我自己能装**
  做完：`CatalogService.canInstallRestricted` 在 Service 入口判断，
  设置里有「材料口令」输入框；带 `restricted` 标记的包没口令就装不上。
  真正的第一道防线是这种包根本不往 CDN 上放。

---

## 阶段 D · 功能（这时每个都只是加一个 Service 加一屏）

- [x] **D1 AI 拆解**（手机端 `CoachService`，提交 `3b2f8f0`）
      ——「8445 网页上填 key」那半条属于 PC 网页端，用户已明确要求不再动那边，
      所以 key 改成在 App 的设置里填。

- [ ] ~~D1b 8445 上的 API key 页面~~（用户封了 PC 网页端，转由 App 内设置承担）
  做：DeepSeek / OpenRouter；key 在网页上填、能测可用性；额度走 `EntitlementService`。
  验收：填错 key 有明确报错；免费用户超额有拦截；断网时降级成只给 GOP 结果不崩。

- [x] **D2a 用户导入的完整渠道**
  做完：`Services/ImportService.swift`（抽音轨 → 分块 → 本机听写 → 分句 →
  本机强制对齐 → 切句音频 → 生成 pack.sqlite，**整条流水线在手机上**）；
  `Stores/UploadServer.swift`（手机自己开 HTTP 口，电脑浏览器直接传，不过服务器）；
  `Screens/Catalog/ImportScreen.swift` 四条路。
  百度网盘 / Google 云盘走系统「文件」选择器 —— 这两个 App 都往文件 App 注册了位置，
  **不用接它们的 SDK**（接了要带闭源库和它们的隐私清单，审核和体积都是代价）。
  验收：分句逻辑有单元测试钉住（分出来的句子必须过难度闸，不许出两个词的碎片）；
  真机上跑一遍完整导入还没做 —— 需要真实音频文件，列在下面「还差的」里。

- [x] **D2 分级材料列表 + 按身份推荐**（提交 `7a577ac`）
  做：小学课本 / 雅思 / TED / 日常口语 / VOA 慢速 / VOA 标准；可预览再决定下不下。
  验收：选身份 → 列表跟着变；每个包都有难度标；预览不用先下整包。

- [x] **D3 分级听力训练（七个练法）**
  做完：`Services/TrainKit.swift`（把词级对齐拆成弱读/连读/重读/意群，纯逻辑）、
  `Services/TrainModes.swift`（七个练法的出题与判卷，纯逻辑）、
  `Services/TrainService.swift`（取题、难度闸、记账）、
  `Screens/Train/`（首页 + 一个通用的练习壳子），「训练」升为一级 tab。
  难度闸：`common5000.txt`（36KB，公开词频表，只有词没有释义）+ 句长 ≤ 18 词。
  验收结果：新增 `LingoTests` 单元测试 target（不用开模拟器，几秒跑完）+
  `TrainUITests` 5 条真机式交互测试；CI 里加了「单元测试（练法逻辑）」这一步。
  ——具体跑过几条、什么时候跑的，以 G0 那一轮的日志为准，别在这儿写死数字。
  原验收：练法逻辑能单元测试（不用开模拟器）；出题句子难度符合下面这个**能机器判**的判据 ——
  句长 ≤ 18 词，且 90% 以上的词落在常用词表（暂定 COCA 前 5000）内。
  （用户原话是"就像朗文词典里的例句差不多，绝大部分人能够的着"，
  不定成数字这条验收就是空的。数字可以再调，但必须有。）

- [x] **D4 会员 / 额度 / 奖励 / 分享海报**（代码部分）
  做完：`EntitlementService`（身份 + 五种额度 + 一次性解锁 + 说人话的超额提示）、
  `Purchases`（StoreKit 2，本机验交易，断网也知道你是会员）、
  `RewardService`（达标发额度、连续七天加一笔、徽章）、
  `MemberScreen` + `QuotaBlockedView` + `PosterView`（ImageRenderer 出图，
  分享走系统面板，不接任何一家社交 SDK）。
  验收结果：额度与奖励逻辑有单元测试（跨周期归零、解锁只扣一次、
  免费 AI 七天试用到期、广告奖励封顶、奖励一天只发一次）。
  **还差**：StoreKit 沙盒里三种真的买通 —— 要用户的付费开发者账号在
  App Store Connect 建商品，属于「要花钱/要账号授权」那一类，得他来。

- [x] **D5 教程 + 配套练习**
  做完：`Screens/Learn/`，八课，大白话。
  例子**不预录**：给一个词组，到用户自己装的材料里现找一句真的来放
  （还能只放那个词组、0.7 倍速）—— 讲「the 只有 80 毫秒」的时候直接听见。
  每课末尾一个按钮直接开练对应的练法。
  改了当初的落点：不放 201 而是随 App 走 —— 用户说过「能独立集成到app最好」，
  而且这跟「完全脱离服务器」是同一个方向。

- [x] **D6 YouTube 词级跟随**
  研究结论（截图在 **cd1 的 `~/cc-claude/doc/ting1/`**）：它不需要音频也能做到词级跟随，
  因为 **YouTube 自动生成的字幕本身就带词级时间戳** —— `fmt=json3` 拿回来的
  `segs[].tOffsetMs` 就是每个词的毫秒偏移。那个「AI 精校字幕要等两分钟」是另一回事：
  人工上传的字幕只有整句时间，要重新对齐才准，所以慢。
  做完：`Stores/YouTube.swift`（视频 id 解析、字幕轨发现、json3 解析、当前词二分）、
  `Screens/Video/VideoFollowScreen.swift`（官方 iframe 播放器 + JS 回报进度 +
  逐词高亮 + 点词跳转 + 重听这句 + 0.75 倍速）。
  视频流是浏览器自己去拿的，**我们不下载、不转存、不代理**，符合用户定的边界。
  两种模式如实标注：自带词级时间戳的说「准的」，只有整句的说「按词长估的，会有偏差」。
  验收结果：json3 解析、按词长摊开、当前词查找都有单元测试钉住。

- [x] **D7 隐私政策 + 用户协议**
  做完：`legal/隐私政策.md`、`legal/用户协议.md`。
  隐私政策里明写"这不只是承诺"——源码里没有上传录音的路径，
  每次构建自动检查（`guard-privacy.sh`），加回来构建就红。

---

## 界面返工（按真机截图逐条来，2026-09-11）

**做完并在真机上验过的**：
- [x] 竖屏波形太高 —— 他两次说太高。360 → 300/420 → **一律 260**。
      空白不靠"把波形抻长"填（那是拿一个毛病盖另一个毛病）。
- [x] 波形顶部**词标签整片消失** —— 原来"宽度不够就整个不画"，
      短词一个字没有，前半句只剩一排竖线。改成缩字号（10.5→9→8）
      再不行只画首字母，每个词都留记号。
- [x] 控制条第一个键不是播放（违反他定的高频顺序）；`− A + − B +` 四个加减撤掉
- [x] 打分四键改成**听完一遍**才浮出（自动播默认开着，按"开始播"算等于没改）
- [x] 跟读结果里**逐词标色提到第一屏**（原来排在长诊断后面，屏幕上一个词都看不见）
- [x] 材料页同一个包上下重复出现；「开始学」按钮图标丢失；分隔线只画半截
- [x] **断网时装好的材料整段消失**，而报错文案还写着"已经装好的包在下面" ——
      新增「已经装好的」一段，永远显示；配了会真失败的断言
- [x] 练法题面缩在顶上、下面空四分之三屏 → 垂直居中
- [x] 盲听填空**把句首也挖了**（"___ match went…"），句首永远不挖

**界面小账，两条都做完了（2026-09-11 晚）**：
- [x] 练法题面正中偏上：只拿上面 78% 的高度去居中，上下空白约 4:6。
      云端 `train-light-usepack-modeblank` 复看：题面上移约 50pt，重心抬起来了。
      题面比这段高时照旧往下长、照旧能滚。
- [x] 精听台不显示文字时那块空白：引导文字确实居中占住了（云端 `drill-light` 复看）。

## iPad 也能当真机跑了（2026-09-11 晚）

接上了 **iPad Pro 12.9 第 3 代（iPad8,5 / iPadOS 26.6.2）**，真机闸从此可以挑设备：
`echo iPad > ~/lingo-dev`（一次性，读完就删；不写就还是 iPhone 11 Pro Max）。
一次接通踩了四步，缺一步就装不上：
1. 线要直插 Mac 自己的 USB-C 口、要能传数据；
2. `xcrun devicectl manage pair --device <id>` 配对（iPad 上点信任）；
3. iPad 设置 → 隐私与安全 → **开发者模式** 打开并重启 —— 这一项**配对之后才会出现**；
4. Xcode → Settings → Accounts 里**要登着 Apple ID**，否则新设备进不了签名档
   （报 `No Accounts` + `provisioning profile doesn't include the currently selected device`）；
   装完还要在 iPad 上「设置 → 通用 → VPN 与设备管理」信任开发者。

**iPad 上跑出来的两件事**（都还没修，见下）：
- `RealFlowUITests` 挂在「找不到 TabBar」：iPadOS 26 把标签栏放成**顶部浮动条**，
  不是 iPhone 那个底部 TabBar，测试按 TabBar 找元素就抓瞎。其余交互测试全过。
- 竖屏排版是把手机版拉长：波形封顶 260 在 2732 高的屏上显得很细，
  文字卡下面空掉一大片。横屏反而正好（波形铺满、控制条在侧边）。

## 用户自己的音频，文字和波形对得准不准（2026-09-12）

> 他的原话：「校对不能留给人工做，别人是拿来学习的，不是来打工的，这事只能机器做，
> 而且要做好。」「不管用什么办法，文本和音频一定要精准对齐。」

**结论：现在是准的。** 真机（iPad Pro 12.9）上拿真人语音考卷量出来：

| 指标 | 结果 | 闸门 |
|---|---|---|
| 文本错误率 | **1.8%**（225 个词里换 2 漏 2） | ≤ 2% |
| 词起点误差 | 中位 **28ms**，p90 41ms | 中位 ≤ 50ms |
| 词终点误差 | 中位 29ms，p90 43ms | — |
| 速度 | 107 秒音频跑 67 秒（0.62x 实时） | — |

### 怎么量（换机器/改代码之后照着跑一遍）

```bash
# 考卷：40 句朗文真人例句 + 服务器对齐好的词级时间，放在 Mac 上，不进仓库
scp 191:/opt/dict/... ~/lingo-benchpack.zip        # 见下面"考卷怎么造"
ssh mac "touch ~/lingo-local; \
  printf '%s\n%s\n' '-demo -usepack -importbench' 700 > ~/lingo-bench; \
  echo iPad > ~/lingo-dev; : > ~/lingo-out.log; touch ~/lingo-go"
ssh mac 'cat ~/lingo-out.log'    # 看 BENCH 结果 / BENCH 判定
```

考卷怎么造：从 `191:/opt/packs/longman-a1.zip` 取前 40 句（`sentences` + `words` 两张表
一起拷），manifest 的 `db_sha256`/`audio_bytes` 重算，**必须打成不压缩的 zip**
（App 里的解压器只认存储式，压缩了会报 `compressed("pack.sqlite")`）。
这是朗文的真人音频，只用来量精度，不进仓库、不进上架包。

### 一路上修掉的五个真问题（每一个都靠这把尺子才看见）

1. **考卷本身是假的**：随包那 12 句测试音频是自己合成的，频谱只有一串谐波、
   3k 以上几乎没能量，Apple 的识别器直接回"没听到人说话" —— 拿它量等于白量。
   换真人语音之后才第一次量出真数字。
2. **长音频对齐按比例猜词数**：老的 `alignLong` 每 8 秒一窗、按"这窗占总时长的比例"
   估该分几个词，窗口按时间走、词按比例走，走着走着脱节，音频走完词还剩一大半，
   循环一结束全扔了（225 个词只对出 71 个）。改成**锚点由声音定**：每窗把剩下的词
   整个喂给 DP，只认结束时间离窗尾 0.8 秒以上的词，下一窗从最后一个认下的词的结尾接着开始。
3. **断句靠标点**：本机听写不给标点，原来只能"每 18 个词硬切一刀"，还按词数依次
   分配（取不满就 break），后一半的词整个丢掉。改成**按词间停顿断句**（≥0.38 秒断一句，
   超 18 词就在最长停顿处再劈，碎片并进旁边）。
4. **iOS 听写器会安静地少给你几十个词**：同一段十几秒音频，真机上 37 词 / 7 词 / 3 词
   交替出现，它不报错。改成**按声学判断认没认全**（这段有几秒在说话，就该有几个词，
   按 1.2 词/秒 卡），不够就重来。
5. **有的段落是确定性地认不全**（重来三次都一样）。最后一招：**从这段最长的那处静音
   劈成两半分别听** —— 剩下那 13 个漏词全是这么找回来的。

另外：整条流水线先按停顿把音频切成 8 秒上下的小段再听写，一举两得 ——
听写器不会中途收工，对齐也正好落在单窗（模型输入就是 8 秒）里。

### 还欠着的

- 剩下 2 个换词、2 个漏词（同音词和吞音），要再降得靠换更强的识别模型
  （whisper small.en 大概 200MB，得第一次导入时下载），先不做。
- 这把尺子**进不了云端 CI**（要真机 + 版权音频），只能手动跑，改动涉及
  `ImportService` / `Aligner` / `Speech` 时必须跑一遍。

## 需要用户拍板的（我不自己决定）

- [ ] Google 广告在国内 iOS 上到底能不能正常展示 —— 他说"先记下研究清楚之后再说"
- [ ] 各档会员具体给多少额度 —— 他说"具体的以后再说订多少"
- [ ] 上架、定价、软著、ICP 备案的时间点

---

## 已知欠账（G3 需求对账查出来的，还没排进上面）

- `Api.recognize`（服务器 whisper）已无人调用，等 B4 一起清掉。
- 多账号隔离目前是**服务器侧**实现（`practice.py` 按账号校验），跟"脱离服务器"
  方向相反，B 阶段掉头时要重做。
- 服务器 `/api/rec` 端点和 `/opt/dict/rec` 下的 33 个历史录音：
  要区分「PC 网页版（我自己用，可留）」和「App（必须无路径）」，
  这个区分要写进 PLAN/ARCHITECTURE，否则以后又会含混。
- 「材料」tab 现在直接就是朗文查词页，还没有版权红线的入口判断（见 C3，
  但入口闸要在 D2 之前先加，越晚补绕过它的调用点越多）。
- D6 要先研究「每日英语听力」，参考截图 PLAN 说在 `ting1` 目录 ——
  在 191 上没找到，开工前先确认这批图还在不在。
