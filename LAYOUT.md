# 精听台布局规则（动手改之前先满足这张表）

屏幕竖着分五段，**上下两头钉死，中间自适应**：

```
顶栏      34            固定
────────────────────────────
波形      min(190, 中间区 × 0.45)    随屏高缩，横屏也放得下
选区条    34            固定，放不下横向滚
原文卡    吃掉剩余（不小于 96）      长句在卡内滚，绝不截断
小句区    82（有小句才占）           两排以上在区内滚
────────────────────────────
打分      46            固定
控制条    ~100          固定
```

**为什么这么定**：中间只有"原文卡"一个弹性件，其余全是常数 ——
所以**同一部手机上，换句子时每一块的位置和高度都不变**（切句乱跳就是因为
以前用弹簧居中，句子长短一变整块就弹）。而长句子不截断，是因为唯一的弹性件
自己会滚。

## 必须同时成立的情况（每次改完逐条对）

| 情况 | 要求 | 靠什么保证 |
|---|---|---|
| 一行的短句 | 卡片不塌、文字靠上 | 卡片最小高 96，内容 topLeading |
| 五六行的长句 + 字号 48 | 全看得到 | 卡片内部 ScrollView |
| 小句 1 排 / 3 排 | 上面的波形不动 | 小句区固定 82，超出在内部滚 |
| 切上下句 | 每块位置不变 | 中间只有一个弹性件，其余是常数 |
| 录完音出结果 | 不顶动上面的布局 | 结果浮在底部一层（overlay），可关 |
| 小屏（iPhone SE 667pt） | 波形不挤没 | 波形取 min(190, 中间区×0.45) |
| 横屏 | 同上 | 同上 |
| 改字号/开关英文释义 | 布局跟着变但不跳 | 卡片是弹性件，边界由屏幕决定不由内容决定 |
| 没有小句（没切词） | 不留空洞 | 小句区仅在有小句时才占位 |

## 别再犯的三条

1. 不要用 Spacer 把内容"居中"——内容一变高度就弹。
2. 不要给会变长的东西写死高度——要么弹性、要么内部滚。
3. 一排按钮的理想宽度可能超过屏宽，会把整屏顶出去——横排一律限宽或横滚。

## 手势分区（2026-09-08 定的，别再往回改）

6.5 寸手机单手拿着，**顶部三分之一是够不到的地方**。所以：

| 区域 | 归谁 | 说明 |
|---|---|---|
| 顶栏 | 只显示"在练哪个词第几句" | 不放任何高频操作 |
| 波形 | 拖＝画/移选区，双指＝缩放 | **不接切句手势**，两个手势打架必输 |
| 选区条 + 原文 + 译文 + 小句 | 左右滑＝上/下一句 | 往左滑是下一句（跟翻书同向） |
| 底部一排 | 播放、循环、录音、收藏、难点 | 拇指区 |
| 底部第二排 | `☰ 3/12`（开句子列表）+ 倍速 | 列表以前在顶栏，够不着 |

切句一共三条路，缺一不可：**滑动**（主力）、**音量键**（锁屏走路时）、**句子列表**（跳着挑）。

滑动的判定：横向位移 > 48pt 且 > 纵向的 1.5 倍 —— 不然长句子在卡片里上下滚会被误判成切句。
滑动没有按钮那种手感，所以必须给回应：轻震一下 + 中间闪一下"4 / 12"。
这个提示要用**独立的 state**，不能复用 flash：换句子时 `.task` 会把 flash 清掉，提示还没看见就没了。

## 横屏（2026-09-08 加的）

判据就一条：`geo.size.width > geo.size.height` 走 `landscape()`，否则 `portrait()`。

横屏唯一稀缺的是**高度**（iPhone 11/15 Pro Max 横屏可用约 393pt）。
所以各块的**最小**高度加起来必须留得下，波形是唯一的弹性件：

| 块 | 最小高 |
|---|---|
| 顶栏 | 34 |
| 波形 | 120（弹性，实际吃掉剩余的全部，约 170+） |
| 选区条（含收藏/难点） | 36 |
| 原文卡 | 60，最多 0.24×高 |
| 控制条（列表/播放/整句/铺满/循环/间隔/录音/倍速） | 36 |
| 打分行 | 40（竖屏是 48） |
| 间距 5×4 | 20 |
| 合计 | ≈346，剩下的归波形 |

给波形写死一个大 minHeight（比如 150）会让总和超过 393 —— 顶栏被挤出屏幕、
打分行被标签栏压住。第一版就是这么翻车的。小句那排横屏不显示（竖屏有）。

**怎么自查横屏**：模拟器命令行转不了屏，用 `-demo -land`：
App 按横屏宽高渲染再整体转 90 度，截出来的图 `ffmpeg -vf transpose=2` 转回来看。
注意别在这个包装上写 `.ignoresSafeArea()`，否则内容贴边、还压住标签栏，
会把没问题的布局误判成有问题。
