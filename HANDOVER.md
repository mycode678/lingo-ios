# 交接文档 · 听说训练台 iOS

写给下一个接手的人（也可能是几个月后的我自己）。
读完这一份，应该能独立把这个 App 继续做下去。

最后更新：2026-09-10（D3 七个练法做完那一轮）

---

## 一、这是个什么东西

一个 iPhone 英语**精听 + 跟读**的 App。核心场景是**走路时练**：
屏幕黑着、单手、用音量键切句子。

一句话怎么被练：
听 → 听不懂就在波形上圈出那一小段反复听 → 录下自己念的 →
跟原声**逐词比对**（每个词准不准、原声多长你多长、该连读的连了没）→
按记忆曲线安排下次复习。

**产品的根本目标：完全脱离服务器。** 对齐、识别、打分、进度全在手机上算，
材料包从 CDN 下下来，装完断网也能练。

---

## 二、先读这四份文档（顺序别乱）

| 文档 | 位置 | 讲什么 |
|---|---|---|
| `PLAN.md` | **191:/opt/dict/PLAN.md** | 产品方案，**引用块里全是用户原话**。唯一准绳。 |
| `ARCHITECTURE.md` | 仓库根 | 分层、三个库怎么分、材料包格式、四步走的路径 |
| `TASKS.md` | 仓库根 | 17 个任务，哪些打了勾、每条的验收标准 |
| `WORKFLOW.md` | 仓库根 | 四道闸怎么走、什么时候才能叫用户、防自欺条款 |

**规矩**：涉及"当初定的是什么"，一律翻 `PLAN.md`，不许凭记忆复述。
以用户**最后一次说的**为准；被覆盖的旧版本文档里保留一行说明，不删。

---

## 三、代码在哪、怎么摆

仓库：`github.com/mycode678/lingo-ios`（私有）
本机镜像：`/home/claude/gitbackup/lingo-ios.git`

```
ios/Sources/
├── App/        LingoApp 入口 · AppEnv 依赖注入 · Demo 演示数据 · DBSelfTest 库自检屏
├── Engines/    全在本机算：Player 播放 · Aligner CTC 对齐(CoreML) · Speech 离线识别
│               · Compare 逐词打分与诊断 · Recorder 录音 · VolumeKeys 音量键切句 · NowPlaying 锁屏
├── Stores/     DB 本机 SQLite · Cache 音频缓存 · PackZip 解材料包 · Api 服务端(正在降级成可选)
├── Services/   PracticeService 进度排期 · CatalogService 材料包 · CoachService AI 拆解 · Store 全局状态
│               · TrainKit 拆句(弱读/连读/重读/意群)+难度闸 · TrainModes 七个练法 · TrainService 取题记账
├── Screens/    Today/ · Catalog/(材料库+查词) · Drill/(精听台+跟读结果+复习+随身) · Train/(七个练法) · Mine/
└── UI/         Theme 设计 token · WaveView 波形 · TextStyle · Audit 布局体检
```

**上层只能调下层**，界面不许直接碰 SQLite、不许直接 `Api.xxx`。

---

## 四、已经做完的（都过了闸门）

**架构**
- A1 文件按分层归位
- A2 本机 SQLite（`Stores/DB.swift`）+ `AppEnv` 注入。库版本 v3，九张表。

**脱离服务器（B 阶段）**
- B1 练习进度与复习排期 → 本机算本机存（SM-2 简化版，见下）
- B2 收藏、难点 → 本机读写
- B3 录音 → 存 `Documents/Recordings/`，索引落本机库，每句只留最近 3 条
- B4（上）断网演练：`-nonet` 启动时所有请求立刻失败，核心流程必须照常

**材料包（C 阶段）**
- C1 打包工具（跑在 **191**）：`/opt/dict/tools/mkpack.py` 出包、`checkpack.py` 验包
- C1b 中文译文：`buildcn.py` 建 `cnmap.sqlite`，全库覆盖 99.4%
- C2 App 端装包：`PackZip`（自写最小 zip 读取器）+ `CatalogService`
- C3 受限材料口令闸（判断在 Service 入口，不是界面藏）

**功能**
- D1 AI 拆解（`CoachService`，DeepSeek / OpenRouter）
- D2 材料库：按身份挑、按难度挑、可预览
- **D3 分级听力训练（七个练法）** —— 用户说的最大竞争力，见下面「七个练法」一节
- D7 隐私政策 + 用户协议（仓库 `legal/`）

---

## 四之二、七个练法（D3）怎么搭的

**为什么别家做不了**：不是技术难，是**没有全库的词级时间戳**。有了它，
"母语者哪个词一带而过、哪儿连读、哪儿断句、哪几个词是重音"全是算出来的，
不用人工标一条。规则和阈值都在 `TrainKit`，跟跟读打分 `Compare` 共用同一套数字 ——
两边标准不一致的话，用户会觉得 App 自相矛盾。

| 练法 | 治什么病 | 怎么出题 |
|---|---|---|
| ① 盲听填空 | 每个词都认识，连起来不认识 | 专挖被弱读的虚词（不是随机挖） |
| ② 意群断句 | 不知道哪儿该断 | 去掉空格，让用户点停顿，跟真实停顿比 |
| ③ 只听重读 | 想听清每个词，结果什么都没抓住 | 重读词原样、其余压到 20% |
| ④ 速度阶梯 | 跟不上速度 | 0.6→0.8→1.0→1.2，听懂才升，记你卡在哪档 |
| ⑤ 听音辨词 | 眼睛认识、耳朵不认识 | 放一个词的原声四选一，干扰项挑长得像的 |
| ⑥ 整句听写 | 综合 | 编辑距离对齐后逐词比，还标出错在连读点上的 |
| ⑦ 影子跟读 | 输出 | 跳精听台（那儿已经做透了，不重造） |

**难度闸**（用户原话"绝大部分人能够的着"落成机器能判的数）：
句长 ≤ 18 词，且 ≥90% 的词在 `Resources/common5000.txt` 里。
词表是公开词频表的前 5000 个，**只有词、没有释义**，跟朗文那份有版权的内容无关。
材料不够时**宁可少给几道题也不用难句凑数** —— 凑数正是用户批评别家的那件事。

**逻辑全是纯函数**（不碰界面、不碰库、不用单例），所以有了 `LingoTests` 这个
单元测试 target：改阈值、改挖几个空、改难度线，跑一遍几秒钟就知道破没破。

---

## 五、还没做的

见 `TASKS.md` 里没打勾的。按当初讨论的轻重：

1. **D2a 用户导入的完整渠道** —— 本机/iCloud/浏览器上传/百度网盘/Google 云盘/播客
2. **D4 会员 + 额度 + 奖励 + 分享海报** —— 连续包月/连续包年/买断三种
3. **D5 教程 + 配套练习** —— 理论结合实际、拿真音频举例、可点击播放
4. **D6 YouTube 词级跟随** —— 只做词级跟随界面，音视频在用户自己手机上；
   要先研究「每日英语听力」怎么做的（参考截图在 **cd1 的 `~/cc-claude/doc/ting1/`**，
   11 张 PNG；HANDOVER 早先写"在 191 上没找到"是找错机器了）

**待用户拍板的**：Google 广告在国内 iOS 能不能用、各档会员给多少额度、
上架/定价/软著/ICP 的时间点。

---

## 六、怎么验（这一节最重要）

### G0 机器闸 —— 云端模拟器，每次推送自动跑

`.github/workflows/shots.yml`（名字叫「G0 验证（模拟器）」）：

```
隐私红线守卫 → 编译 → 单元测试（练法逻辑）→ 两台模拟器截图 → 布局体检 → 交互测试 → 传产物
```

看结果（不需要 gh CLI）：
```bash
GH=<token>
curl -s -H "Authorization: token $GH" \
  "https://api.github.com/repos/mycode678/lingo-ios/actions/runs?per_page=5" \
  | python3 -c "import sys,json
for r in json.load(sys.stdin)['workflow_runs']:
    print(r['id'],r['name'],r['status'],r.get('conclusion'))"
```
日志：`/actions/jobs/<job_id>/logs`，产物在 run 页面的 artifacts。

**云端跳过两条测试**（原因写在 workflow 注释里）：
- `AlignerUITests` 要 181MB 的 CoreML 模型，模型不进仓库
- `ShotUITests` 是给真机导截图用的，模拟器上会报 background assertion 失败

### 真机（只在需要时）

用户的 Mac Air（ssh 别名 `mac`）+ iPhone 数据线。脚本 `~/bin/lingo-agent.sh`，
用一次性标志文件触发：

```bash
ssh mac 'echo <提交号> > ~/lingo-want; touch ~/lingo-test ~/lingo-shot ~/lingo-go'
```

`~/lingo-want` 是**防自欺装置**：编译的必须是这个提交，对不上直接停。
（吃过亏：GitHub 拉不到时脚本悄悄拿上一版编译测试截图，全绿，
但改的东西根本没进去。）

Mac Air **开不了模拟器**（2019 双核 8G，试过，负载冲到 156 把 SSH 挤断），
它只负责编译和驱动真机。

### 布局体检

`UI/Audit.swift`：`-demo -audit` 启动时，App 自己把每一块的真实坐标报出来，
重叠或超出屏幕就 `LAYOUT-FAIL` → **测试红**。
坐标用 `.named("drill")` 坐标系，不能用 `.global`（横屏截图是转 90 度画的，
global 坐标跟着转，报出来全是假重叠）。

### 三个审查 agent

`.claude/agents/` 下：`code-critic`（只找会真出错的缺陷）、
`ui-critic`（截图+体检数字，按 HIG 和用户的设计原则挑）、
`spec-auditor`（拿 PLAN.md 对代码，产出差集）。跨会话可用。

---

## 七、设计决定与它们的理由（别随手推翻）

**复习排期不照搬 Anki 的 SM-2**（`PracticeService.grade`）
这是听力不是背单词。听懂过一次不代表下次听得懂，间隔涨得慢一档；
"没听懂"是 **10 分钟后**就再撞一次（还在这次练习里），不推到明天 ——
走路时练，一次通勤就该把没听懂的多撞几遍。

**材料包一律不压缩（ZIP_STORED）**
iOS 没自带解 zip 的 API，而开发原则写了不引不必要的第三方库。
全部 stored 的话解包器只有几十行、没依赖、不会出错。
mp3 本来压不动，sqlite 那点收益不值当 —— 传输时让 CDN 在网络层压。

**词边界随包预先算好**
手机算得动（跟服务器差 6 毫秒），但一个包几百句现算要等很久。
用户下下来就该能练。用户自己导入的材料才在手机上现算。

**三个库分开存**（`packs/*.sqlite` 只读 / `user.sqlite` 可写 / `Recordings/`）
删材料包只删那个目录和一行记账，**用户数据一条不动** ——
进度收藏难点录音记的是句子 id，包装回来它们还在。

**苹果的识别只用来听写，对齐还得用 wav2vec2**
听写是"猜你说了什么"，对齐是"拿原句去卡你的录音"。
苹果那个只能告诉你它自己认出来的词大概在哪，你念错了就跟原句对不上号。
而"哪怕你念得不准，也要知道 the 你念在第几毫秒"才是逐词打分的地基。

**AI 拆解不把音频发出去**
发的只有"哪个词、原声多长、你多长、该连没连"这些数字。
录音永远不出这台手机 —— 这条是方案红线，`ios/guard-privacy.sh` 在 CI 里守着，
只要 App 代码里重新出现上传录音的路径，构建就红。

**划了选区就只练这一段**
参考文本和参考词边界要一起裁到选区，时间轴平移到"选区开头＝0"。
只裁音频不裁文本的话，你念了三个词却拿整句去对齐，结果必然是乱的。

---

## 八、踩过的坑（重复踩的代价很大）

| 坑 | 现在靠什么挡住 |
|---|---|
| GitHub 拉不到，脚本悄悄拿上一版编译测试截图，全绿但改动没进去 | `~/lingo-want` 校验提交号，对不上直接停 |
| 布局体检量出了重叠但只 print 不断言，测试照样绿 | `LAYOUT-FAIL` 进探针标签 → 测试断言 |
| 横屏滚动测试盯的元素改完顺序后一进来就在屏幕里，一次不用滑就通过 | 只盯**最后一个**元素；断言必须真的可能失败 |
| 体检探针只写在竖屏，横屏（最容易挤重叠）永远"没拿到"还不算失败 | 探针两个方向共用；拿不到 = 红 |
| 同一台模拟器上先跑截图后跑测试，截图那轮写进 UserDefaults 的偏好把测试带红 | `-demo` 下每次启动把偏好写死，起始状态永远一致 |
| 源码归位到子目录后，"文件清单变了才重新生成工程"的判断用的是 `ls`，看不到子目录里的新文件 | 改成 `find` 递归 |
| 横屏 `onAppear` 把 `@AppStorage` 的显示偏好写成 false，转回竖屏永久丢失 | 改用独立 `@State` |
| `layoutPriority` 写在 `padding` 里面等于没写 | 必须是最外层修饰符 |
| `.frame(width: geo.size.width)` 横屏含刘海区，比安全区宽 88 点 | 用 `.frame(maxWidth: .infinity)` |
| 播放器回调捕获的是 View 的值拷贝，读开关拿到旧值 | 现取 `UserDefaults` |
| 长按挂在 `Button` 上不触发（Button 吃手势） | 普通 View + `contentShape` + 手势 |
| `/opt/dict` 的 git 里混进 1.3G 朗文词典源文件（有版权） | `.gitignore` 排掉，历史已重开 |

---

## 九、机器与账号

| 名字 | 是什么 | 干什么 |
|---|---|---|
| `191` | 192.168.8.191 | 词典服务 8445、对齐引擎 `/opt/align`、材料包 `/opt/packs`、打包工具 `/opt/dict/tools` |
| `mac` | 用户的 Mac Air | 编译 + 驱动真机。开不了模拟器 |
| 云端 CI | GitHub Actions macOS runner | 模拟器测试、截图、布局体检 |
| `cd1` | 我跑命令的机器 | 本机 git 镜像 `/home/claude/gitbackup/` |

**注意：PC 网页端（`191:/opt/dict/app`、`srv`、nginx）用户已明确要求不要再动。**
材料包打包工具在 `/opt/dict/tools/`，那是给 App 用的，跟网页端无关，可以继续用。

---

## 十、用户定的硬性规矩

这些他说过"不再提醒"，必须自觉遵守：

1. **该我干好的活自己搞好**，不能一次次让他发现问题。**他只看结果，别讲原因。**
2. 交付前**自己把真实用户流程跑通**，不是打接口验、不是看代码验。
3. **优先用模拟器**做自动化测试，能自动测的不许让他手工测。
4. 汇报只讲五点：完成了什么 / 自动测了什么 / 是否全部通过 / 还有什么问题 / 下一步。
   **全部正常就直接继续下一阶段，不要停下来等确认。**
5. 手机 App 设计：拇指区优先、手势优先、高频操作不进二级菜单、图标要配字、
   一行放不下就横滑不许折两行。
6. 只有六种情况才问他：产品需求有重大歧义、要 Apple 开发者账号操作、
   需要真机才能验的硬件功能、需要真实 API Key、需要真实音频/课程内容、涉及付费或账号授权。
